#include "main.h"
#include <stdio.h>
/**
* main: print program name
* @argc: argument count
* @argv[]: string
* Return: nothing 
*/
int main(int __attribute__((unused)) argc, char *argv[])
{
  printf("%s", argv[0]);
return (0);
}
