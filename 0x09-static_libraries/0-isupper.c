#include "main.h"
#include <stdio.h>
/**
* _isupper - check for uppercase characters
*@c: character to be checked
* Return: Always 0.
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'B')
{
return (1);
}
else
{
return (0);
}
}
